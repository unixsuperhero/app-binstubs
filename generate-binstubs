
find $1 -name '.git' | while read app
do

  app_dir=$(dirname ${app})
  app_name=$(basename ${app_dir})

  cat <<BINSTUB >"${2}/${app_name}"
#!/bin/bash

export APP_NAME="${app_name}"
export APP_DIR="${app_dir}"

\$(dirname \$0)/base-binstub \$@
BINSTUB

done

